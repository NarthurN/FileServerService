type: object
properties:
  error:
    type: object
    properties:
      code:
        type: integer
        example: 500
      text:
        type: string
        example: "Внутренняя ошибка сервера"
    required:
      - code
      - text
required:
  - error
