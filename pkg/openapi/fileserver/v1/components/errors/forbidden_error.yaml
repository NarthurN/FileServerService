type: object
properties:
  error:
    type: object
    properties:
      code:
        type: integer
        example: 403
      text:
        type: string
        example: "Нет прав доступа"
    required:
      - code
      - text
required:
  - error
