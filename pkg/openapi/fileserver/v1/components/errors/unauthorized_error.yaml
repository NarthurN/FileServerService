type: object
properties:
  error:
    type: object
    properties:
      code:
        type: integer
        example: 401
      text:
        type: string
        example: "Не авторизован"
    required:
      - code
      - text
required:
  - error
