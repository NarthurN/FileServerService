type: object
properties:
  error:
    type: object
    properties:
      code:
        type: integer
        example: 404
      text:
        type: string
        example: "Ресурс не найден"
    required:
      - code
      - text
required:
  - error
