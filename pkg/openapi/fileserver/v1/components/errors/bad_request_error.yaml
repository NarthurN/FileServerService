type: object
properties:
  error:
    type: object
    properties:
      code:
        type: integer
        example: 400
      text:
        type: string
        example: "Некорректные параметры"
    required:
      - code
      - text
required:
  - error
